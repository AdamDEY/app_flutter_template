class Routes {
  // Private constructor to prevent instantiation
  Routes._();

  //-- Main Tabs --
  static const home = '/home';
  static const profile = '/profile';

  //-- Other Screens (Future usage) --
  static const login = '/login';
  static const settings = '/settings';
}
