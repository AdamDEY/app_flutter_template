# 🧭 Router

**Navigation Configuration.**

- `app_router.dart`: The main `GoRouter` configuration. Handles the Bottom Navigation Shell and Navigator Keys.
- `routes.dart`: Static string constants for route paths (e.g., `static const home = '/home';`).
- `home_shell.dart`: The persistent wrapper widget that holds the Bottom Navigation Bar.
